import * as React from 'react';
import { AppLayout } from '#layouts';
import { SingleCharacterCard } from '#pods/single-character';

export const SingleCharacterScene = () => (
  <AppLayout>
    <SingleCharacterCard />
  </AppLayout>
);
